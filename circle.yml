machine:
  pre:
    - mkdir -p $HOME/.sbt/.lib/1.2.3 && wget http://central.maven.org/maven2/org/scala-sbt/sbt-launch/1.2.3/sbt-launch-1.2.3.jar -O $HOME/.sbt/.lib/1.2.3/sbt-launch.jar
  java:
    version: oraclejdk8
